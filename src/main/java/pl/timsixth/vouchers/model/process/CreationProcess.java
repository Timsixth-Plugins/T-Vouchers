package pl.timsixth.vouchers.model.process;

import java.util.UUID;

public class CreationProcess extends AbstractProcess{

    public CreationProcess(UUID userUUID) {
        super(userUUID);
    }
}
